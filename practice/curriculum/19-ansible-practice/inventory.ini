# Ansible Inventory — Local practice
# For remote hosts, replace 'ansible_connection=local' with SSH details

[local]
localhost ansible_connection=local

[local:vars]
ansible_python_interpreter=/usr/bin/python3
app_dir=/opt/app
app_port=8080
postgres_user=labuser
postgres_password=labpass
postgres_db=labdb
app_version=1.0.0
