# Exercise 13.B.3 + 13.C.1 — Reverse Proxy + Load Balancing
# Guide: docs/curriculum/13-nginx-reverse-proxy.md

# TODO: Uncomment and customize

# events {}
# http {
#     # Exercise 13.C.1 — Load balancing (uncomment for multiple app instances)
#     # upstream app_servers {
#     #     server app1:8000;
#     #     server app2:8000;
#     #     server app3:8000;
#     # }
#
#     # Exercise 13.C.3 — Rate Limiting
#     # limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;
#
#     # Exercise 13.C.6 — Gzip Compression
#     # gzip on;
#     # gzip_types text/plain application/json;
#
#     server {
#         listen 80;
#
#         # Reverse proxy to app
#         location / {
#             proxy_pass http://app:8000;
#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header X-Forwarded-Proto $scheme;
#         }
#
#         # Exercise 13.C.3 — Rate limiting on API endpoints
#         # location /api/ {
#         #     limit_req zone=api burst=20 delay=10;
#         #     proxy_pass http://app:8000;
#         # }
#
#         # Exercise 13.B.4 — Static files
#         # location /static/ {
#         #     alias /usr/share/nginx/html/static/;
#         #     expires 30d;
#         # }
#
#         # Health check (no rate limiting)
#         # location /health {
#         #     proxy_pass http://app:8000;
#         # }
#     }
# }
