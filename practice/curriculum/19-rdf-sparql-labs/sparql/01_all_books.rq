PREFIX dct:    <http://purl.org/dc/terms/>
PREFIX schema: <http://schema.org/>

# SPARQL equivalent of SQL Q1: All books with authors
SELECT ?title ?author
WHERE {
    ?book a schema:Book ;
          dct:title ?title ;
          schema:author ?authorNode .
    ?authorNode schema:name ?author .
}
ORDER BY ?title
